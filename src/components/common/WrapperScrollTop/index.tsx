import type { ReactNode } from "react";
import ScrollTop from "../ScrollTop";

const WrapperScrollTop = ({ children }: { children: ReactNode }) => {
  <ScrollTop />;
  return <>{children}</>;
};

export default WrapperScrollTop;
